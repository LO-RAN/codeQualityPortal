# home directory of the tomcat installation
catalina.home=D:/Tomcat5.5

# directory path where the output of the build is placed
build.home=.

# directory path of the optimalflow.jar file
optimalflow.jar=./lib

# directory path of the unet.jar file
unet.jar=${optimalflow.jar}

# directory path of the mifdoclet.jar file, not required
# only needed for target mifdoc
mifdoclet.jar=C:/mifdoclet1.2/lib
